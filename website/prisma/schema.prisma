generator client {
    provider     = "prisma-client-js"
    transactions = false
}

datasource db {
    provider = "mongodb"
    url      = env("DB_CONNSTR")
}

model ID {
    id String @id @default(auto()) @map("_id") @db.ObjectId
}
